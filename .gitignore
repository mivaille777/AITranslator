AITranslator/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/*
!dist/AITranslator/
!dist/AITranslator/**
coverage.xml
.coverage
test-results/
apps/desktop/test-results/

logs/*
!logs/.gitkeep

# Per-user settings and runtime databases are local state and must never be committed.
config/user.toml
config/*.sqlite3
config/*.sqlite3-shm
config/*.sqlite3-wal
# Legacy JSON is no longer read, but keep any old local file untracked.
config/chat_history.json

# Local LangSmith/LangGraph credentials and developer overrides.
.env
.env.*
!.env.example

*.log

# Private development plan; keep it local and never publish it.
Windows桌面划词翻译软件_架构技术路线与Codex分步提示词.md

# Agent runtime

.langgraph_api/

# Local runtime data

data/
models/
.cache/

# RAG local storage

config/rag/

# Python runtime

**pycache**/
*.pyc

# Serialized runtime state

*.pkl
*.pickle
